pub mod animations;
pub mod chart;
pub mod chart_models;
pub mod document;
pub mod geometry;
mod graphics;
pub mod html;
pub mod subscribers;

use wasm_bindgen::prelude::wasm_bindgen;

#[wasm_bindgen]
extern "C" {
    #[wasm_bindgen(js_namespace = console)]
    pub fn log(s: &str);
}

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
